import 'package:flutter/material.dart';

/// A placeholder class that represents an entity or model.
class SampleItem {
  final int id;
  final Color color;
  final String text;

  const SampleItem(this.id, this.color, this.text);
}
